Bug Tracker
by Charles Weiss (2021)

The purpose of this project was to build a basic bug-tracker which would run as a web application.

As of writing this the program uses C# in the back-end with the .NET framework.
HTML and CSS are used in the front end to render the web-application.

